
# lists
# List of fruits
fruits = ["apple", "banana", "cucumber"]
print(fruits[0])

# IndexError

# this line of code will throw an error because of the out of bound index error
# the size of the list of fruits is three, but remember the index starts at 0, then is 0 to 2.
print(fruits[3]) 
